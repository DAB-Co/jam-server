{
  "name": "jam-server",
  "version": "1.0.0",
  "description": "node js server for jam",
  "main": "jam_server_main.js",
  "scripts": {
    "test": "echo http_port=8080 > .env.local && node overwrite_database.js && bash run.sh && ((mocha --recursive --exit && bash kill.sh) || (echo --output file-- && cat output && bash kill.sh))"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DAB-Co/jam-server.git"
  },
  "author": "DAB Co.",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/DAB-Co/jam-server/issues"
  },
  "homepage": "https://github.com/DAB-Co/jam-server#readme",
  "dependencies": {
    "@dab-co/jam-sqlite": "^3.2.1",
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "yargs": "^17.3.0"
  },
  "devDependencies": {
    "axios": "^0.24.0",
    "mocha": "^9.1.3"
  }
}
